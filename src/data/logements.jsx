//import data from '../logements.json'

const array = [1, 2, 3]
function Data() {
    return (
        <div>
            {array.map((item) => console.log(item))}
        </div>
    )
}

export default Data